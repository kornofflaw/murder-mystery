# Room photos

Upload room images here. Name each file as shown on the prompts page
(for example `train-crane.jpg`). If a name is wrong, Claude will sort it out.
A room only switches to its photo once Claude has marked the click areas
for it in `web/js/photos.js`.
